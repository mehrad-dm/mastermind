---
context: <name>
---

# Lessons — <name>

Lessons specific to **this app/package**, captured by `levelup`. They stay here and never
leak to another context. When a lesson turns out to be true for the whole stack — not just
this app — promote it up into the field's `stack-defaults.md`, where every app benefits.

_(none yet — this fills in as MasterMind does real work in this context.)_
