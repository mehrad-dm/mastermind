# Learning Sources — how to learn this field well

The discipline for getting up to speed fast and correctly (pairs with the `learn` skill).

- **Prefer primary sources** — official docs, specs, source code — over blog posts and memory.
- **Check dates & versions** — this field moves; verify against the version actually installed.
- **Learn the *why*, not the syntax** — the mental model transfers; the API does not.
- **World-class exemplars to read:** <the reference codebases/projects that show the bar in this field>.
- **Fold durable lessons back** into `stack-defaults.md` / `lessons.md` via `levelup`.
