# Journal: what happened, dated, one line each

Episodic memory (`engineering/core/rigor.md`). Appended at each verdict. Entries marked as misses
(date, then the word between middot separators) are the calibration record, read back with
`mastermind wrong-log` and distilled by `levelup`. This sentence deliberately does not spell that
marker out: when it did, every naive count of the file included the sentence describing it.

2026-08-04 · agent-callable brain (skills/skill/agents/agent/route/wrong-log) · Cursor and Codex had no native skill mechanism, so an agent inlined the library or guessed paths · ship: 19 assertions, live-verified on Claude Code, Cursor and Codex
2026-08-04 · wrong · claimed route-then-skill cost 97% less than reading the index · caught by evals/agent-surface-cost.mjs after route was redesigned to return the full table: it is ~41% MORE than the index path · measure the shipped design, never the prototype, before quoting a number
2026-08-04 · wrong · shipped keyword routing as a ranked shortlist and called it accurate · caught by evals/agent-surface-routing.mjs on organically-phrased requests: 0% top-1, 25% top-3 (it sent "the invoice screen takes nine seconds" to roadmap) · test with phrasing that does not reuse the trigger words the ranker scores against
2026-08-04 · wrong · a flag before the subcommand fell through to the install path, so `mastermind --json skills` would clone and write · caught by code-reviewer, reproduced with MASTERMIND_HOME=/nonexistent · parse the command anywhere in argv, and assert the read-only promise in a test
2026-08-04 · wrong · large piped output was truncated mid-string, producing invalid JSON · caught by evals/agent-surface-routing.mjs failing to parse at ~8KB · process.exit() discards unflushed async stdout: use writeSync when the process exits immediately
2026-08-04 · wrong · shipped the lookup shim only in the shared clone · caught by a live Claude Code session refusing to execute a path outside its sandbox · ship the runnable surface inside the project brain, where sandboxed agents actually run
2026-08-04 · wrong · the wrong-log was not discoverable: the kernel pointed at rigor.md without naming the command · caught by a live session that answered "check git log" instead · if a capability has a name, name it where the agent reads
2026-08-04 · wrong · wrong-log counted 7 misses when 6 were logged · caught by counting its output against the file: the unanchored filter matched the journal header's own sentence about `· wrong ·` lines · anchor a log filter to the entry format, and test it against a file containing prose about the format
2026-08-08 · hardened the installer's filesystem boundary + release gates after a deep review · ship: 149 installer, 26 surface, 14 preflight gates
2026-08-08 · wrong · fixed routes.map traversal on the install side only, left the uninstall side wide open · caught by reproducing sibling-directory deletion during uninstall · when a rule guards untrusted input, fix EVERY path that reads it in the same change, and test each one
2026-08-08 · wrong · guarded only the listed engine paths against symlinks while the copy loop wrote deeper files · caught by overwriting an external SKILL.md through .mastermind/skills/build/SKILL.md · guard the operation, not the list of names above it
2026-08-08 · wrong · left the .manifest read unguarded after guarding routes.map · caught by deleting ../precious.txt via a committed manifest · every file inside a project repo is untrusted input, not just the one you were thinking about
2026-08-08 · wrong · my test suite repointed the developer's real ~/.mastermind twice, once leaving it dangling · caught by the user's own broken install · a test that runs the installer must sandbox HOME, and the suite now trips an alarm if the real symlink moves
2026-08-08 · wrong · replaced the measured router saving (20,870 -> 7,220, -65%) with an invented total*0.25 ratio and shipped it as "75% fewer tokens" · caught by reading sync-evals.mjs · a number with no run behind it is decoration; generate from the artifact that measured it
2026-08-08 · wrong · put a phrase into quarantine's routing description that my own eval used as its test prompt · caught by re-reading the diff before trusting the score · a test that reuses the wording it is testing measures copy-paste, not behaviour
2026-08-08 · wrong · my infrastructure detector scanned stdout, so sessions merely discussing "unauthorized" code were reported as auth failures · caught by 3/8 sessions failing while a manual claude call succeeded · classify failures from the channel that reports them, not from model output
2026-08-08 · wrong · wrote a levelup eval case that referred to a correction from an earlier turn a single-shot session cannot have · caught by reading the session transcript instead of the score · a case that needs context the harness cannot provide is a defective check, not a finding
2026-08-08 · wrong · uninstall silently removed nothing whenever the project path crossed a symlink: is_ours compared a resolved link target against a logical BRAIN path · caught by inspecting what a fresh end-to-end uninstall actually left on disk instead of trusting its success message · compare resolved paths, and assert on the filesystem rather than on the tool's own report
2026-08-08 · wrong · reported "a fresh npx mastermind-brain installs 0.29.0" after a run that passed MASTERMIND_HOME explicitly: a real user passes nothing, and for them the install aborted with "refusing to link ~/.mastermind to itself" · caught by reproducing the documented command on macOS and two Linux images · reproduce the DOCUMENTED command verbatim; every flag you add to make a test convenient is a flag the user will not type
2026-08-08 · wrong · the self-link guard compared path STRINGS, so every sandbox under /tmp or mktemp's /var/folders (both symlinks to /private/...) took the healthy branch and the broken one never ran · caught by re-running on a real HOME · when a check compares paths, resolve both sides: and give the test a canonical path, or it proves the opposite of what it claims
2026-08-08 · wrong · shipped an isolated brain without scripts/ while init told the model to run node scripts/build-router.mjs · caught by letting a real init run 38 turns and ~$1.86 before it dead-ended on the missing module · if an instruction names a file, the install that receives that instruction must contain the file: assert it in the install test, not in review
2026-08-08 · wrong · the Alpine CI step asserted the no-bash guard against `skills`, a read-only lookup that needs a BRAIN rather than bash, so it saw "no brain found" and proved nothing · caught by running the tagged workflow in node:22-alpine · point a guard test at the code path that owns the guard, and assert the specific message
2026-08-09 · wrong · my live-tool check grepped for "mastermind" and passed the sentence "I'm running as Codex, not MasterMind": a denial scored as a success · caught by reading the actual answer instead of the ✓ · assert on the CLAIM, not on a word that appears in both the affirmative and the negative
2026-08-09 · wrong · the same check wired the probe project with `claude cursor`, omitting `agents`: so AGENTS.md never existed and Codex was asked to load a brain it could not see · caught by the denial above, once the assertion stopped hiding it · a probe must assert its own preconditions before it asks the question, or a broken setup reads as a broken product
2026-08-09 · wrong · told the user the wrong-log held 26 entries: it holds 22 · caught by counting it before repeating the number, prompted by the user asking me to explain the claim · never quote a count from memory when the command that produces it is one call away
2026-08-09 · wrong · the installer followed a repo-supplied `.claude`/`.cursor` symlink and wrote 28 files outside the project, exit 0 · caught by creating the symlink and looking at where the files landed · containment must guard the OPERATION's every write target, not the one directory I was thinking about when I wrote the last containment fix
2026-08-09 · wrong · claimed "you always know exactly what ran" while verification sat inside `if (.git exists)`, so a brain with no git history was executed unverified by the signed release · caught by dropping an arbitrary install.sh into ~/.mastermind and watching it run · a guard nested inside an existence check only guards the case that exists; verify or refuse, never fall through
2026-08-09 · wrong · shipped 28 absolute symlinks and absolute hook paths under a comment promising "project-relative, never absolute" · caught by cloning the project elsewhere and moving the original away · a contract written in a comment is decoration until a test clones the repo and resolves every link
2026-08-09 · wrong · my own verify-tools.sh ran the installer with the real $HOME and repointed a real ~/.mastermind at a temp dir · caught by another test's tripwire · I wrote "a test that runs the installer must sandbox HOME" in this log days earlier and then did it again; a lesson that is not enforced by a check is a lesson I will repeat
2026-08-09 · wrong · guarded integration DIRECTORIES against redirection and left the individual files inside them wide open, so .cursor/rules/mastermind.mdc, .mastermind/VERSION, .manifest and .installed were still written outside the repo · caught by symlinking each owned file rather than its parent · resolving a file's parent is not resolving the file; follow the link itself, and cover the owned files as a list rather than the directories that contain them
2026-08-09 · wrong · treated a failed `git status` as a clean working tree, so a corrupted index turned the dirty-engine check into a pass while HEAD still verified · caught by writing garbage into .git/index and watching the install proceed · "cannot tell" is not "clean"; a verification step that catches its own error has stopped being one
2026-08-09 · wrong · the install record was replaced on every run, so repairing one tool erased the memory of the others and the doctor called a broken install healthy again · caught by installing three tools, repairing one, deleting a third tool's file and getting exit 0 · a record that is rewritten wholesale is not a record; merge on write, remove only on uninstall
2026-08-09 · wrong · uninstall left our appended pointer in .claude/CLAUDE.md and AGENTS.md, because that file was missing from the cleanup list and the match only looked for the shared-clone wording while isolated installs write a different one · caught by installing over a user's own files and diffing them after uninstall · when the same text has two forms, the cleanup has to know both, and the list of files has to match the list we write to
2026-08-09 · wrong · a regex I ran across the test file corrupted nine unrelated assertions, and I only noticed because tests that had nothing to do with the change went red · caught by reading the failure names instead of the failure count · a bulk edit over a file needs its blast radius checked before the next command, not after the suite disagrees
2026-08-10 · wrong · preserved new engine-file collisions only when a prior hash ledger existed, so a first install could overwrite a project-owned file or directory at an engine path · caught by the isolated code reviewer with first-install, directory, and prefix-sibling fixtures · ownership must be proven by an exact ledger field; missing or ambiguous metadata means preserve
2026-08-10 · wrong · targeted uninstall rewrote a stale digest and still removed shared hooks/anchors, while agents and codex aliases could delete each other's AGENTS wiring · caught by the isolated code reviewer and the full installer matrix · calculate the post-uninstall tool set before cleanup, then remove only surfaces no surviving tool owns
2026-08-11 · wrong · reported "immutable Releases are not available on this repository, verified via the API" after only grepping the repo object for an immutable_releases field · caught by the external reviewer calling GET /repos/{owner}/{repo}/immutable-releases, which returns {"enabled":false,"enforced_by_owner":false} · a field missing from one endpoint's payload is not the feature's absence; when a capability has its own documented endpoint, call it before reporting on it, and never label an inference "verified"
2026-08-11 · wrong · wrote `git show ... | grep -q` in the new pre-push tag guard, so under pipefail the writer's SIGPIPE returned 141 and a perfectly valid tag was refused for a changelog section that was right there · caught by the external reviewer running the hook against the real v0.31.7 · I had already replaced this exact construct in verify-release.sh and verify-tools.sh weeks earlier; `grep -q` ends a pipeline early by design, so never put it downstream of anything under pipefail, use a here-string
2026-08-11 · wrong · put the tag guard after the ALLOW_SENSITIVE bypass, so a flag meant for skipping the secret scan also waved through an irreversible tag, and let an unresolvable target or unreadable origin/master continue instead of refusing · caught by the external reviewer enumerating the early-exit paths · a guard for an irreversible action belongs above every bypass, and every branch it cannot evaluate must refuse, not continue
